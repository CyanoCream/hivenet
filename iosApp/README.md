# iOS App

This folder contains the Swift entrypoint for the Compose Multiplatform UI.

The Xcode project should link the KMP framework generated by Gradle from `:composeApp`.
After Java/JDK is available, run the Gradle iOS framework task or open the generated project from your IDE workflow.

Connected device detected during setup:

- `Cak Anang ™️ (26.5)`
